/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["demo.themesberg.com", "flowbite.s3.amazonaws.com", "flowbite.com", "img.clerk.com"],
  },
};

export default nextConfig;
